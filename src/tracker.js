'use strict';

(function () {
  // @exclude
  var WebSocket = require('websocket').w3cwebsocket;
  // @endexclude

  function Tracker(url) {
    var _url = url;

    return {
    };
  }

  module.exports = Tracker;
})();
